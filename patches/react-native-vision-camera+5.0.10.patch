diff --git a/node_modules/react-native-vision-camera/android/build.gradle b/node_modules/react-native-vision-camera/android/build.gradle
index e6c990f..3387bf5 100644
--- a/node_modules/react-native-vision-camera/android/build.gradle
+++ b/node_modules/react-native-vision-camera/android/build.gradle
@@ -169,6 +169,10 @@ dependencies {
 
   // Add a dependency on NitroImage
   implementation project(":react-native-nitro-image")
+
+  // camera-video uses Media3MuxerImpl at runtime but declares media3-muxer as
+  // compileOnly, so it is not included in the APK transitively. Add it explicitly.
+  implementation "androidx.media3:media3-muxer:1.8.0"
 }
 
 tasks.register('prepareHeaders', Copy) {
